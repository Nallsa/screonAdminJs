export interface PlaylistItem {
    id: number;
    title: string;
    image: string;
    type: string;
    duration: number;
}

export interface LibraryItem {
    id: number;
    title: string;
    image: string;
}